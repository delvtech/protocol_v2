import { waffle } from "hardhat";

const { provider } = waffle;

describe("ERC4626YieldAdapter", async () => {
  // let asset: MockAsset;
  // let assetOwner: SignerWithAddress;
  // let token: ERC20;
  // before(async () => {
  //   [assetOwner] = await ethers.getSigners();
  //   token = await new MockAsset__factory().connect(assetOwner).deploy();
  // });
  // beforeEach(async () => {
  //   await createSnapshot(provider);
  // });
  // afterEach(async () => {
  //   await restoreSnapshot(provider);
  // });
  // it("wwww", async () => {
  //   console.log(token.address);
  //   console.log(await token.totalSupply());
  // });
});
